import React from "react";

// auth middleware
import Auth from "../utils/auth";

// components
import Landing from "../components/Landing/Landing";

const Home = () => {
  return (
    <Landing />
  )
};

export default Home;
